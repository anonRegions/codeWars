/* Complete the solution so that it returns a String in the format:
 *
 * "Key (Value), Key (Value)"
 *
 from a hash for values within the given range, sorted by value
 lowest to highest. */
function list_in_range(key, range) {

}
